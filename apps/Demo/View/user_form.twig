<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>User Form</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<header>KoruPHP Demo</header>
<div class="container">
<h1>{% if user %}Edit User{% else %}Add User{% endif %}</h1>
<form method="POST" action="{{ action }}">
    {% if user %}
        <input type="hidden" name="id" value="{{ user.id }}">
    {% endif %}
    <input type="text" name="username" placeholder="Username" value="{{ user.username|default('') }}" required>
    <input type="password" name="password" placeholder="Password" {% if not user %}required{% endif %}>
    <button type="submit">Save</button>
</form>
<p><a href="/users">Back to list</a></p>
</div>
</body>
</html>
